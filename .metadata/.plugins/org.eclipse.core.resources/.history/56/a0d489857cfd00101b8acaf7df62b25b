package aaa;

import java.util.concurrent.Executor;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.scheduling.annotation.EnableAsync;
import org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor;

@EnableAsync	// 비동기 사용 설정 가능 - @Async 동작 가능케함
@Configuration
public class AsyncConfig {
	@Bean	// Executor 빈 등록
			// @Async 메소드는 Executor를 사용해서 실행
	public Executor taskExecutor() {
		
		// Thread Pool 기반 비동기 Executor 생성
		ThreadPoolTaskExecutor res = new ThreadPoolTaskExecutor();
		
		// 기본 유지 스레드 수
		// 기본 유지 - 항상 존재, 요청 없이도 유지, 초기 로딩시 생성
		res.setCorePoolSize(5);
		
		// 최대 생성 스레드 개수
		// CorePoolSize + queue가 꽉 찼을 때 추가로 생성, 이 숫자를 넘으면 작업 거절
		res.setMaxPoolSize(20);
		
		return res;
	}
}
