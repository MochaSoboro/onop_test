package aaa.control;

import java.util.Map;

import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import aaa.JwtUtil;
import aaa.model.LoginDTO;

@RestController
public class LoginController {
	
	private final JwtUtil jwtUtil;
	
	public LoginController(JwtUtil jwtUtil) {
		this.jwtUtil = jwtUtil;
	}

	@PostMapping("/login")
	Map<String, String> login(@RequestBody LoginDTO dto) throws Exception {
		System.out.println("백엔드 로그인 진입: "+dto);
		
		if (dto.getPid().equals("aaa") && dto.getPw().equals("1111")) {
			String token = jwtUtil.createToken(dto.getPid());
			
			
			return Map.of("token", token);
		}
		
		throw new Exception("인증되지 않음");
	}
	
	@RequestMapping("/hello")
	String hello() {
		System.out.println("hello 진입");
		return "hello ";
	}
}
